# دليل نسخ النظام لمطعم جديد

نظام النقط/الأختام/الكروت واحد وثابت — كل اللي بيتغير من مطعم للتاني هو **الهوية** (الاسم، الألوان، اللوجو، الفروع، أرقام الواتساب، روابط السوشيال ميديا) و**قاعدة البيانات** (كل مطعم منفصل تمامًا عن التاني، مفيش أي تداخل بيانات).

الوقت المتوقع لنسخة جديدة: **أقل من ساعة** لو اتبعت الخطوات بالترتيب.

---

## 1) الباك إند (Google Sheets — الوضع الحالي)

- [ ] اعمل Google Sheet جديد فاضي (منفصل تمامًا عن شيت شيخ البلد)
- [ ] افتح Extensions → Apps Script في الشيت الجديد
- [ ] الصق نفس كود `Code.gs` (بدون أي تعديل في المنطق نفسه)
- [ ] غيّر في أول الكود:
  ```js
  const CONFIG = {
    SHEET_ID: '...',          // ID الشيت الجديد (من رابط الشيت)
    ADMIN_PASSWORD: '...',    // كلمة سر جديدة قوية — مش نفس كلمة سر شيخ البلد
    APP_NAME: 'اسم المطعم الجديد',
    ...
  };
  ```
- [ ] Deploy → New deployment → Web app → انسخ الرابط (Script URL)
- [ ] شغّل `runOnce_addBranchColumn` مرة واحدة من محرر الأبس سكريبت (لو مضفتها)

> **لما ننقل لـ Supabase مستقبلًا:** نفس الـ SQL schema هيتكرر لكل مطعم بمشروع Supabase منفصل — نفس الفكرة بالظبط.

## 2) الفرونت إند — `config.js` (أهم ملف)

انسخ المشروع كامل في مجلد جديد، وعدّل الملف ده بس (باقي الكود متلمسوش خالص):

```js
appName:        'اسم المطعم الجديد',
appTagline:     'نقاط الولاء',           // أو أي وصف قصير
checkoutText:   'ادفع فاتورتك في أي فرع من فروع اسم المطعم',
websiteUrl:     'https://domain-المطعم-الجديد.com',

colors: {
  primary:     '#...',   // اللون الأساسي (هوية المطعم)
  primaryDark: '#...',
  accent:      '#...',   // لون الذهبي/التمييز
},

logoUrl:        'icon-192x192.png',   // نفس الاسم، بس الصورة نفسها لازم تتغير (خطوة 3)

whatsappAdmin:    '201xxxxxxxxx',
whatsappSupport:  '201xxxxxxxxx',

branches: [
  { key: 'branch1', name: 'اسم الفرع الأول' },
  { key: 'branch2', name: 'اسم الفرع التاني' },
  // ... حسب فروع المطعم الجديد
],

social: {
  facebook:  '...',
  tiktok:    '...',
  instagram: '...',
},

scriptUrl: '...',   // رابط الـ Apps Script الجديد من خطوة 1

copyrightYear:  '2026',
copyrightText:  'البيانات محمية ومشفرة',
```

## 3) الأيقونات واللوجو

- [ ] استبدل `icon-192x192.png` (الجذر) بلوجو المطعم الجديد، **نفس الاسم بالظبط**، مقاس 192×192، PNG حقيقي
- [ ] استبدل `icons/icon-192x192.png` و`icons/icon-512x512.png` بنفس الطريقة (512×512 للتاني)
- [ ] لو غيرت أسماء الملفات، لازم تحدّث `manifest.json` و`sw.js` (قائمة `PRECACHE_ASSETS`) بنفس الأسماء الجديدة

## 4) الاستضافة

- [ ] ريبو GitHub جديد منفصل (أو مجلد فرعي منفصل لو هتستخدم Vercel)
- [ ] لو GitHub Pages: تأكد إن `manifest.json` فيه `start_url: "./"` و`scope: "./"` (نسبي، مش مطلق) عشان يشتغل مهما كان الدومين
- [ ] لو هتنقل لـ Vercel: اربط الريبو الجديد بمشروع Vercel منفصل

## 5) اختبار قبل التسليم

- [ ] سجّل دخول عميل جديد وجرب: نقط، أختام، كروت، صدارة
- [ ] سجّل دخول أدمن بكل الأدوار (سوبر أدمن، كاشير فرع، كول سنتر) وجرب كل تاب
- [ ] ثبّت التطبيق على موبايل (Add to Home Screen) وتأكد إنه بيفتح صح
- [ ] تأكد إن اللوجو والاسم والألوان ظاهرين صح في: الصفحة الرئيسية، صفحة الدخول، لوحة الأدمن، رسالة المشاركة (واتساب)

---

## حاجات لازم تفتكرها دايمًا
- **كل مطعم = Google Sheet منفصل + Script URL منفصل + كلمة سر أدمن منفصلة.** مفيش أي مشاركة بيانات بين المطاعم خالص.
- متنساش تغيّر `ADMIN_PASSWORD` الافتراضية (`1234`) — دي أهم خطوة أمان في أي نسخة جديدة.
- لو ضفت فيتشر أو صلحت باج في نسخة (زي شيخ البلد)، الكود مش بينتقل تلقائي للنسخ التانية — لازم تنسخه يدوي لحد ما نبني نظام تحديث مركزي (مرحلة تانية لو عدد المطاعم زاد).
